# Alembic schema reconciliation

> Generated by a read-only inspection of the Supabase `public` schema. No DDL, DML, Alembic stamp, migration, or managed-schema operation was executed.

## Executive summary

- Current Alembic revision: **none** (`alembic_version` table present: `false`).
- Alembic head: **20260711_0010**.
- Expected application tables: **34**.
- Actual `public` application tables: **34**.
- Matching table names: **34**.
- Exact table definitions: **0**.
- Tables containing data: **0**.
- Exact individual expected objects: **332**.
- Missing or differently defined expected objects: **169**.
- Unexpected actual objects: **121**.
- Total mismatch count: **290**.

Counting method: individual objects are columns, named primary/unique/check/foreign-key constraints, indexes, enums, identity sequences, the application search function, and the required extension. A changed definition counts once as an expected mismatch; an actual object with a name absent from Alembic counts once as unexpected. Extension-owned `pg_trgm` functions/operators are represented by the extension object and are not counted individually.

## Recommendation

**Approach A ? clean rebuild of application-owned objects in `public`** is recommended, subject to explicit approval.

Reasons:

- All 34 application tables are empty.
- No Alembic revision matches the current structure exactly, so an exact baseline stamp is unsafe.
- The schema differs across constraint names, foreign-key shapes, checks, indexes, two required columns, and the search function.
- A reconciliation migration would preserve no application data and would be more complex and riskier than rebuilding the empty application schema.

The rebuild must drop only the 34 listed application tables, 14 application enum types, and the RecallStack search function. It must not drop the `public` schema, `pg_trgm`, or any Supabase-managed schema/object.

Data loss: no application rows currently exist, so the observed application-row loss would be zero. A fresh backup is still mandatory because row counts can change after this report.

## Migration revision evidence

| Revision | Group | Structural evidence | Conclusion |
|---|---|---|---|
| `20260711_0001` | identity | All 3 tables and columns exist, but primary/FK names and required grant indexes differ. | **Not exact** |
| `20260711_0002` | catalog and taxonomy | All 4 tables and `topic_kind` exist, but constraint and index definitions/names differ. | **Not exact** |
| `20260711_0003` | content | All content tables/enums exist, but constraints and indexes differ from the migration definitions. | **Not exact** |
| `20260711_0004` | learning | All 4 tables and enums exist, but foreign-key names, checks, and indexes differ. | **Not exact** |
| `20260711_0005` | practice | All 3 tables and `practice_outcome` exist, but resource uniqueness, attempt FKs, and indexes differ. | **Not exact** |
| `20260711_0006` | recall | Both tables and `review_rating` exist, but keys/indexes differ. | **Not exact** |
| `20260711_0007` | sync | All 8 tables and 3 enums exist, but keys/indexes differ. | **Not exact** |
| `20260711_0008` | attempt idempotency | `practice_attempts.attempt_event_id` and its unique constraint are missing. | **Absent** |
| `20260711_0009` | publisher attribution | `content_versions.published_by`, its FK, check, and partial index are missing. | **Absent** |
| `20260711_0010` | catalog search | `pg_trgm`, the trigram index, and function exist, but the function omits both same-domain join predicates required by the migration. | **Partial, not exact** |

No revision is safe to stamp based on structural equality.

## Table inventory and row counts

| Table | Rows | Exact final definition |
|---|---:|---|
| `activity_events` | 0 | no |
| `bookmarks` | 0 | no |
| `catalog_release_versions` | 0 | no |
| `catalog_releases` | 0 | no |
| `catalog_sync_change_log` | 0 | no |
| `catalog_sync_counters` | 0 | no |
| `categories` | 0 | no |
| `content_blocks` | 0 | no |
| `content_item_categories` | 0 | no |
| `content_item_topics` | 0 | no |
| `content_items` | 0 | no |
| `content_relations` | 0 | no |
| `content_version_blocks` | 0 | no |
| `content_version_status_history` | 0 | no |
| `content_versions` | 0 | no |
| `device_catalog_sync_state` | 0 | no |
| `device_user_sync_state` | 0 | no |
| `devices` | 0 | no |
| `domains` | 0 | no |
| `practice_attempts` | 0 | no |
| `practice_providers` | 0 | no |
| `practice_resources` | 0 | no |
| `profile_role_grants` | 0 | no |
| `profiles` | 0 | no |
| `review_cards` | 0 | no |
| `review_history` | 0 | no |
| `roles` | 0 | no |
| `sync_mutations` | 0 | no |
| `topic_categories` | 0 | no |
| `topics` | 0 | no |
| `user_notes` | 0 | no |
| `user_progress` | 0 | no |
| `user_sync_change_log` | 0 | no |
| `user_sync_counters` | 0 | no |

## Missing and extra tables

- Expected but missing: none.
- Present but absent from Alembic: none.

## Per-table differences

### `activity_events`

- primary key `pk_activity_events`: expected `(('id',),)`; missing.
- unexpected primary key `activity_events_pkey`: `(('id',),)`.
- foreign key `fk_activity_events_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_activity_events_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'SET NULL')`; missing.
- unexpected foreign key `fk_activity_events_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'SET NULL')`.
- unexpected foreign key `fk_activity_events_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_activity_events_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- index `ix_activity_events_user_occurred`: expected `(False, ('user_id', 'occurred_at desc'), '', '', ())`; missing.
- unexpected index `ix_activity_events_user_recent`: `(False, ('user_id', 'occurred_at', 'id'), '', '', ())`.

### `bookmarks`

- primary key `pk_bookmarks`: expected `(('user_id', 'content_item_id'),)`; missing.
- unexpected primary key `bookmarks_pkey`: `(('user_id', 'content_item_id'),)`.
- foreign key `fk_bookmarks_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_bookmarks_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- unexpected foreign key `fk_bookmarks_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_bookmarks_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_bookmarks_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- unexpected index `ix_bookmarks_recent`: `(False, ('user_id', 'created_at', 'content_item_id'), '', '', ())`.

### `catalog_release_versions`

- primary key `pk_catalog_release_versions`: expected `(('catalog_release_id', 'content_item_id'),)`; missing.
- unexpected primary key `catalog_release_versions_pkey`: `(('catalog_release_id', 'content_item_id'),)`.
- foreign key `fk_catalog_release_versions_item_version`: expected `(('content_item_id', 'content_version_id'), 'public', 'content_versions', ('content_item_id', 'id'), 'RESTRICT')`; missing.
- foreign key `fk_catalog_release_versions_domain_release`: expected `(('domain_id', 'catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'CASCADE')`; missing.
- foreign key `fk_catalog_release_versions_domain_item`: expected `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'RESTRICT')`; missing.
- unexpected foreign key `fk_catalog_release_versions_item_same_domain`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'RESTRICT')`.
- unexpected foreign key `fk_catalog_release_versions_release_same_domain`: `(('domain_id', 'catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'CASCADE')`.
- unexpected foreign key `fk_catalog_release_versions_version_same_item`: `(('content_item_id', 'content_version_id'), 'public', 'content_versions', ('content_item_id', 'id'), 'RESTRICT')`.
- index `ix_catalog_release_versions_domain_item`: expected `(False, ('domain_id', 'content_item_id'), '', '', ())`; missing.
- index `ix_catalog_release_versions_item_version`: expected `(False, ('content_item_id', 'content_version_id'), '', '', ())`; missing.
- index `ix_catalog_release_versions_domain_release`: expected `(False, ('domain_id', 'catalog_release_id'), '', '', ())`; missing.
- unexpected index `ix_catalog_release_versions_content_version`: `(False, ('content_version_id',), '', '', ())`.

### `catalog_releases`

- primary key `pk_catalog_releases`: expected `(('id',),)`; missing.
- unexpected primary key `catalog_releases_pkey`: `(('id',),)`.
- foreign key `fk_catalog_releases_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_catalog_releases_published_by_profiles`: expected `(('published_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- unexpected foreign key `fk_catalog_releases_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_catalog_releases_published_by`: `(('published_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- index `ix_catalog_releases_published_by`: expected `(False, ('published_by',), '', '', ())`; missing.
- unexpected index `ix_catalog_releases_domain_status`: `(False, ('domain_id', 'status', 'release_number'), '', '', ())`.

### `catalog_sync_change_log`

- primary key `pk_catalog_sync_change_log`: expected `(('domain_id', 'cursor'),)`; missing.
- unexpected primary key `catalog_sync_change_log_pkey`: `(('domain_id', 'cursor'),)`.
- foreign key `fk_catalog_sync_change_log_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_catalog_sync_change_log_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`.
- index `ix_catalog_sync_change_log_retain_until`: expected `(False, ('retain_until',), '', '', ())`; missing.

### `catalog_sync_counters`

- primary key `pk_catalog_sync_counters`: expected `(('domain_id',),)`; missing.
- unexpected primary key `catalog_sync_counters_pkey`: `(('domain_id',),)`.
- foreign key `fk_catalog_sync_counters_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_catalog_sync_counters_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`.

### `categories`

- primary key `pk_categories`: expected `(('id',),)`; missing.
- unexpected primary key `categories_pkey`: `(('id',),)`.
- unique constraint `uq_categories_domain_id_slug`: expected `(('domain_id', 'slug'),)`; missing.
- unexpected unique constraint `uq_categories_domain_slug`: `(('domain_id', 'slug'),)`.
- foreign key `fk_categories_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_categories_domain_parent_category`: expected `(('domain_id', 'parent_category_id'), 'public', 'categories', ('domain_id', 'id'), 'RESTRICT')`; missing.
- unexpected foreign key `fk_categories_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_categories_parent_same_domain`: `(('domain_id', 'parent_category_id'), 'public', 'categories', ('domain_id', 'id'), 'RESTRICT')`.
- index `ix_categories_domain_parent`: expected `(False, ('domain_id', 'parent_category_id'), '', '', ())`; missing.
- unexpected index `ix_categories_domain_parent_sort`: `(False, ('domain_id', 'parent_category_id', 'sort_order', 'id'), '', '', ())`.

### `content_blocks`

- primary key `pk_content_blocks`: expected `(('id',),)`; missing.
- unexpected primary key `content_blocks_pkey`: `(('id',),)`.
- foreign key `fk_content_blocks_created_by_profiles`: expected `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- unexpected foreign key `fk_content_blocks_created_by`: `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- index `ix_content_blocks_created_by`: expected `(False, ('created_by',), '', '', ())`; missing.

### `content_item_categories`

- primary key `pk_content_item_categories`: expected `(('content_item_id', 'category_id'),)`; missing.
- unexpected primary key `content_item_categories_pkey`: `(('content_item_id', 'category_id'),)`.
- foreign key `fk_content_item_categories_domain_category`: expected `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`; missing.
- foreign key `fk_content_item_categories_domain_item`: expected `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`; missing.
- unexpected foreign key `fk_content_item_categories_category_same_domain`: `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`.
- unexpected foreign key `fk_content_item_categories_item_same_domain`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`.
- index `ix_content_item_categories_domain_category`: expected `(False, ('domain_id', 'category_id'), '', '', ())`; missing.
- index `ix_content_item_categories_domain_item`: expected `(False, ('domain_id', 'content_item_id'), '', '', ())`; missing.
- unexpected index `ix_content_item_categories_category_sort`: `(False, ('category_id', 'sort_order', 'content_item_id'), '', '', ())`.

### `content_item_topics`

- primary key `pk_content_item_topics`: expected `(('content_item_id', 'topic_id'),)`; missing.
- unexpected primary key `content_item_topics_pkey`: `(('content_item_id', 'topic_id'),)`.
- foreign key `fk_content_item_topics_domain_topic`: expected `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`; missing.
- foreign key `fk_content_item_topics_domain_item`: expected `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`; missing.
- unexpected foreign key `fk_content_item_topics_item_same_domain`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`.
- unexpected foreign key `fk_content_item_topics_topic_same_domain`: `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`.
- index `uq_content_item_topics_one_primary`: expected `(True, ('content_item_id',), 'is_primary', '', ())`; missing.
- index `ix_content_item_topics_domain_topic`: expected `(False, ('domain_id', 'topic_id'), '', '', ())`; missing.
- index `ix_content_item_topics_domain_item`: expected `(False, ('domain_id', 'content_item_id'), '', '', ())`; missing.
- unexpected index `ix_content_item_topics_topic_sort`: `(False, ('topic_id', 'sort_order', 'content_item_id'), '', '', ())`.

### `content_items`

- primary key `pk_content_items`: expected `(('id',),)`; missing.
- unexpected primary key `content_items_pkey`: `(('id',),)`.
- unique constraint `uq_content_items_domain_id_slug`: expected `(('domain_id', 'slug'),)`; missing.
- unexpected unique constraint `uq_content_items_domain_slug`: `(('domain_id', 'slug'),)`.
- foreign key `fk_content_items_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_content_items_created_by_profiles`: expected `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- unexpected foreign key `fk_content_items_created_by`: `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- unexpected foreign key `fk_content_items_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`.
- index `ix_content_items_current_version`: expected `(False, ('current_published_version_id',), '', '', ())`; missing.
- index `ix_content_items_current_ownership`: expected `(False, ('id', 'current_published_version_id'), '', '', ())`; missing.
- index `ix_content_items_public_browse`: expected `(False, ('domain_id', 'type'), 'archived_at is null', '', ())`; missing.
- index `ix_content_items_created_by`: expected `(False, ('created_by',), '', '', ())`; missing.
- unexpected index `ix_content_items_browse`: `(False, ('domain_id', 'type', 'difficulty', 'id'), '', '', ())`.

### `content_relations`

- primary key `pk_content_relations`: expected `(('source_content_item_id', 'target_content_item_id', 'relation_type'),)`; missing.
- unexpected primary key `content_relations_pkey`: `(('source_content_item_id', 'target_content_item_id', 'relation_type'),)`.
- check constraint `chk_content_relation_canonical_symmetric`: expected `("relation_type not in ('related', 'alternative')) or (source_content_item_id < target_content_item_id",)`; different (actual `("(relation_type <> all (array['related', 'alternative'])) or source_content_item_id < target_content_item_id",)`).
- index `ix_content_relations_target`: expected `(False, ('target_content_item_id',), '', '', ())`; missing.
- unexpected index `ix_content_relations_reverse`: `(False, ('target_content_item_id', 'relation_type', 'source_content_item_id'), '', '', ())`.

### `content_version_blocks`

- primary key `pk_content_version_blocks`: expected `(('content_version_id', 'position'),)`; missing.
- unexpected primary key `content_version_blocks_pkey`: `(('content_version_id', 'position'),)`.
- index `ix_content_version_blocks_block`: expected `(False, ('content_block_id',), '', '', ())`; different (actual `(False, ('content_block_id', 'content_version_id'), '', '', ())`).

### `content_version_status_history`

- primary key `pk_content_version_status_history`: expected `(('id',),)`; missing.
- unexpected primary key `content_version_status_history_pkey`: `(('id',),)`.
- index `ix_content_version_status_history_changed_by`: expected `(False, ('changed_by',), '', '', ())`; missing.
- index `ix_content_version_status_history_version`: expected `(False, ('content_version_id',), '', '', ())`; missing.

### `content_versions`

- column `published_by`: expected `('uuid', True, '', False)`; missing.
- primary key `pk_content_versions`: expected `(('id',),)`; missing.
- unexpected primary key `content_versions_pkey`: `(('id',),)`.
- unique constraint `uq_content_versions_content_item_id_id`: expected `(('content_item_id', 'id'),)`; missing.
- unexpected unique constraint `uq_content_versions_item_id_id`: `(('content_item_id', 'id'),)`.
- check constraint `chk_content_version_published_at_by`: expected `("status <> 'published' or (published_at is not null and published_by is not null)",)`; missing.
- foreign key `fk_content_versions_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'CASCADE')`; missing.
- foreign key `fk_content_versions_reviewed_by_profiles`: expected `(('reviewed_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- foreign key `fk_content_versions_authored_by_profiles`: expected `(('authored_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- foreign key `fk_content_versions_published_by_profiles`: expected `(('published_by',), 'public', 'profiles', ('id',), 'RESTRICT')`; missing.
- unexpected foreign key `fk_content_versions_authored_by`: `(('authored_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- unexpected foreign key `fk_content_versions_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'CASCADE')`.
- unexpected foreign key `fk_content_versions_reviewed_by`: `(('reviewed_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- index `ix_content_versions_published_by`: expected `(False, ('published_by',), 'published_by is not null', '', ())`; missing.
- index `ix_content_versions_authored_by`: expected `(False, ('authored_by',), '', '', ())`; missing.
- index `ix_content_versions_reviewed_by`: expected `(False, ('reviewed_by',), '', '', ())`; missing.
- unexpected index `ix_content_versions_status_created`: `(False, ('status', 'created_at', 'id'), '', '', ())`.

### `device_catalog_sync_state`

- primary key `pk_device_catalog_sync_state`: expected `(('device_id', 'domain_id'),)`; missing.
- unexpected primary key `device_catalog_sync_state_pkey`: `(('device_id', 'domain_id'),)`.
- foreign key `fk_device_catalog_sync_state_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`; missing.
- foreign key `fk_device_catalog_sync_state_device_id_devices`: expected `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`; missing.
- foreign key `fk_device_catalog_sync_state_domain_release`: expected `(('domain_id', 'last_catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'RESTRICT')`; missing.
- unexpected foreign key `fk_device_catalog_sync_state_device`: `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`.
- unexpected foreign key `fk_device_catalog_sync_state_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`.
- unexpected foreign key `fk_device_catalog_sync_state_release_same_domain`: `(('domain_id', 'last_catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'RESTRICT')`.
- index `ix_device_catalog_sync_state_domain_release`: expected `(False, ('domain_id', 'last_catalog_release_id'), '', '', ())`; missing.

### `device_user_sync_state`

- primary key `pk_device_user_sync_state`: expected `(('device_id',),)`; missing.
- unexpected primary key `device_user_sync_state_pkey`: `(('device_id',),)`.
- foreign key `fk_device_user_sync_state_device_id_devices`: expected `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_device_user_sync_state_device`: `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`.

### `devices`

- primary key `pk_devices`: expected `(('id',),)`; missing.
- unexpected primary key `devices_pkey`: `(('id',),)`.
- foreign key `fk_devices_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_devices_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_devices_user_id`: expected `(False, ('user_id',), '', '', ())`; missing.
- unexpected index `ix_devices_user`: `(False, ('user_id', 'registered_at', 'id'), '', '', ())`.

### `domains`

- primary key `pk_domains`: expected `(('id',),)`; missing.
- unexpected primary key `domains_pkey`: `(('id',),)`.
- unique constraint `uq_domains_slug`: expected `(('slug',),)`; missing.
- unexpected unique constraint `domains_slug_key`: `(('slug',),)`.

### `practice_attempts`

- column `attempt_event_id`: expected `('uuid', False, '', False)`; missing.
- primary key `pk_practice_attempts`: expected `(('id',),)`; missing.
- unexpected primary key `practice_attempts_pkey`: `(('id',),)`.
- unique constraint `uq_practice_attempts_attempt_event_id`: expected `(('attempt_event_id',),)`; missing.
- check constraint `chk_practice_attempt_conf_before`: expected `('confidence_before is null or confidence_before between 0 and 100',)`; different (actual `('confidence_before is null or confidence_before >= 0 and confidence_before <= 100',)`).
- check constraint `chk_practice_attempt_conf_after`: expected `('confidence_after is null or confidence_after between 0 and 100',)`; different (actual `('confidence_after is null or confidence_after >= 0 and confidence_after <= 100',)`).
- foreign key `fk_practice_attempts_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_practice_attempts_provider_id_practice_providers`: expected `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_practice_attempts_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_practice_attempts_resource`: expected `(('practice_resource_id',), 'public', 'practice_resources', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_practice_attempts_resource_item`: expected `(('practice_resource_id', 'content_item_id'), 'public', 'practice_resources', ('id', 'content_item_id'), 'RESTRICT')`; missing.
- foreign key `fk_practice_attempts_resource_provider`: expected `(('practice_resource_id', 'provider_id'), 'public', 'practice_resources', ('id', 'provider_id'), 'RESTRICT')`; missing.
- unexpected foreign key `fk_practice_attempts_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_practice_attempts_provider`: `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_practice_attempts_resource_context`: `(('practice_resource_id', 'content_item_id', 'provider_id'), 'public', 'practice_resources', ('id', 'content_item_id', 'provider_id'), 'RESTRICT')`.
- unexpected foreign key `fk_practice_attempts_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_practice_attempts_resource_item`: expected `(False, ('practice_resource_id', 'content_item_id'), '', '', ())`; missing.
- index `ix_practice_attempts_user_attempted`: expected `(False, ('user_id', 'attempted_at desc'), '', '', ())`; missing.
- index `ix_practice_attempts_resource_provider`: expected `(False, ('practice_resource_id', 'provider_id'), '', '', ())`; missing.
- index `ix_practice_attempts_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- index `ix_practice_attempts_provider_id`: expected `(False, ('provider_id',), '', '', ())`; missing.
- index `ix_practice_attempts_resource_id`: expected `(False, ('practice_resource_id',), '', '', ())`; missing.
- unexpected index `ix_practice_attempts_resource`: `(False, ('practice_resource_id',), '', '', ())`.
- unexpected index `ix_practice_attempts_user_content_recent`: `(False, ('user_id', 'content_item_id', 'attempted_at', 'id'), '', '', ())`.
- unexpected index `ix_practice_attempts_user_recent`: `(False, ('user_id', 'attempted_at', 'id'), '', '', ())`.

### `practice_providers`

- primary key `pk_practice_providers`: expected `(('id',),)`; missing.
- unexpected primary key `practice_providers_pkey`: `(('id',),)`.
- unique constraint `uq_practice_providers_slug`: expected `(('slug',),)`; missing.
- unexpected unique constraint `practice_providers_slug_key`: `(('slug',),)`.

### `practice_resources`

- primary key `pk_practice_resources`: expected `(('id',),)`; missing.
- unexpected primary key `practice_resources_pkey`: `(('id',),)`.
- unique constraint `uq_practice_resources_id_provider`: expected `(('id', 'provider_id'),)`; missing.
- unique constraint `uq_practice_resources_item_provider_url`: expected `(('content_item_id', 'provider_id', 'url_hash'),)`; missing.
- unique constraint `uq_practice_resources_id_content_item`: expected `(('id', 'content_item_id'),)`; missing.
- unexpected unique constraint `uq_practice_resources_identity_context`: `(('id', 'content_item_id', 'provider_id'),)`.
- unexpected unique constraint `uq_practice_resources_item_provider_url_hash`: `(('content_item_id', 'provider_id', 'url_hash'),)`.
- foreign key `fk_practice_resources_provider_id_practice_providers`: expected `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_practice_resources_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- unexpected foreign key `fk_practice_resources_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_practice_resources_provider`: `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`.
- index `uq_practice_resources_one_primary`: expected `(True, ('content_item_id',), 'is_primary and archived_at is null', '', ())`; missing.
- index `ix_practice_resources_provider_id`: expected `(False, ('provider_id',), '', '', ())`; missing.
- unexpected index `ix_practice_resources_content`: `(False, ('content_item_id', 'sort_order', 'id'), '', '', ())`.
- unexpected index `ix_practice_resources_provider`: `(False, ('provider_id', 'content_item_id', 'id'), '', '', ())`.

### `profile_role_grants`

- primary key `pk_profile_role_grants`: expected `(('id',),)`; missing.
- unexpected primary key `profile_role_grants_pkey`: `(('id',),)`.
- foreign key `fk_profile_role_grants_profile_id_profiles`: expected `(('profile_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_profile_role_grants_role_id_roles`: expected `(('role_id',), 'public', 'roles', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_profile_role_grants_revoked_by_profiles`: expected `(('revoked_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- foreign key `fk_profile_role_grants_granted_by_profiles`: expected `(('granted_by',), 'public', 'profiles', ('id',), 'SET NULL')`; missing.
- unexpected foreign key `fk_role_grant_granted_by`: `(('granted_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- unexpected foreign key `fk_role_grant_profile`: `(('profile_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- unexpected foreign key `fk_role_grant_revoked_by`: `(('revoked_by',), 'public', 'profiles', ('id',), 'SET NULL')`.
- unexpected foreign key `fk_role_grant_role`: `(('role_id',), 'public', 'roles', ('id',), 'RESTRICT')`.
- index `ix_profile_role_grants_granted_by`: expected `(False, ('granted_by',), '', '', ())`; missing.
- index `ix_profile_role_grants_role_id`: expected `(False, ('role_id',), '', '', ())`; missing.
- index `ix_profile_role_grants_profile_id`: expected `(False, ('profile_id',), '', '', ())`; missing.
- index `ix_profile_role_grants_revoked_by`: expected `(False, ('revoked_by',), '', '', ())`; missing.

### `profiles`

- column `timezone`: expected `('varchar(64)', False, 'utc', False)`; different (actual `('varchar(64)', False, "'utc' varying", False)`).
- primary key `pk_profiles`: expected `(('id',),)`; missing.
- unexpected primary key `profiles_pkey`: `(('id',),)`.
- foreign key `fk_profiles_id_users`: expected `(('id',), 'auth', 'users', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `profiles_id_fkey`: `(('id',), 'auth', 'users', ('id',), 'CASCADE')`.

### `review_cards`

- column `scheduler_name`: expected `('varchar(80)', False, 'simple', False)`; different (actual `('varchar(80)', False, "'simple' varying", False)`).
- column `scheduler_version`: expected `('varchar(40)', False, '1', False)`; different (actual `('varchar(40)', False, "'1' varying", False)`).
- primary key `pk_review_cards`: expected `(('id',),)`; missing.
- unexpected primary key `review_cards_pkey`: `(('id',),)`.
- foreign key `fk_review_cards_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_review_cards_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_review_cards_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_review_cards_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_review_cards_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- index `ix_review_cards_due_active`: expected `(False, ('user_id', 'due_at'), 'suspended_at is null', '', ())`; missing.
- unexpected index `ix_review_cards_due`: `(False, ('user_id', 'due_at', 'id'), '', '', ())`.

### `review_history`

- primary key `pk_review_history`: expected `(('id',),)`; missing.
- unexpected primary key `review_history_pkey`: `(('id',),)`.
- unique constraint `uq_review_history_review_event_id`: expected `(('review_event_id',),)`; missing.
- unexpected unique constraint `review_history_review_event_id_key`: `(('review_event_id',),)`.
- foreign key `fk_review_history_card_user`: expected `(('review_card_id', 'user_id'), 'public', 'review_cards', ('id', 'user_id'), 'CASCADE')`; missing.
- unexpected foreign key `fk_review_history_card_owner`: `(('review_card_id', 'user_id'), 'public', 'review_cards', ('id', 'user_id'), 'CASCADE')`.
- index `ix_review_history_card_user`: expected `(False, ('review_card_id', 'user_id'), '', '', ())`; missing.
- index `ix_review_history_user_reviewed`: expected `(False, ('user_id', 'reviewed_at desc'), '', '', ())`; missing.
- unexpected index `ix_review_history_card_recent`: `(False, ('review_card_id', 'reviewed_at', 'id'), '', '', ())`.
- unexpected index `ix_review_history_user_recent`: `(False, ('user_id', 'reviewed_at', 'id'), '', '', ())`.

### `roles`

- primary key `pk_roles`: expected `(('id',),)`; missing.
- unexpected primary key `roles_pkey`: `(('id',),)`.
- unique constraint `uq_roles_code`: expected `(('code',),)`; missing.
- unexpected unique constraint `roles_code_key`: `(('code',),)`.

### `sync_mutations`

- primary key `pk_sync_mutations`: expected `(('mutation_id',),)`; missing.
- unexpected primary key `sync_mutations_pkey`: `(('mutation_id',),)`.
- check constraint `chk_sync_mutation_status_consistency`: expected `("status = 'received' and processed_at is null) or (status = 'applied' and processed_at is not null) or (status = 'rejected' and processed_at is not null and error_code is not null",)`; different (actual `("status = 'received' and processed_at is null or status = 'applied' and processed_at is not null or status = 'rejected' and processed_at is not null and error_code is not null",)`).
- foreign key `fk_sync_mutations_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_sync_mutations_device_user`: expected `(('device_id', 'user_id'), 'public', 'devices', ('id', 'user_id'), 'CASCADE')`; missing.
- unexpected foreign key `fk_sync_mutations_device_owner`: `(('device_id', 'user_id'), 'public', 'devices', ('id', 'user_id'), 'CASCADE')`.
- unexpected foreign key `fk_sync_mutations_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_sync_mutations_expires_at`: expected `(False, ('expires_at',), '', '', ())`; missing.
- index `ix_sync_mutations_device_user`: expected `(False, ('device_id', 'user_id'), '', '', ())`; missing.
- index `ix_sync_mutations_user_id`: expected `(False, ('user_id',), '', '', ())`; missing.
- unexpected index `ix_sync_mutations_status_received`: `(False, ('status', 'received_at', 'mutation_id'), '', '', ())`.
- unexpected index `ix_sync_mutations_user_received`: `(False, ('user_id', 'received_at', 'mutation_id'), '', '', ())`.

### `topic_categories`

- primary key `pk_topic_categories`: expected `(('topic_id', 'category_id'),)`; missing.
- unexpected primary key `topic_categories_pkey`: `(('topic_id', 'category_id'),)`.
- foreign key `fk_topic_categories_domain_category`: expected `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`; missing.
- foreign key `fk_topic_categories_domain_topic`: expected `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`; missing.
- unexpected foreign key `fk_topic_categories_category_same_domain`: `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`.
- unexpected foreign key `fk_topic_categories_topic_same_domain`: `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`.
- index `ix_topic_categories_domain_category`: expected `(False, ('domain_id', 'category_id'), '', '', ())`; missing.
- index `ix_topic_categories_domain_topic`: expected `(False, ('domain_id', 'topic_id'), '', '', ())`; missing.
- unexpected index `ix_topic_categories_category_sort`: `(False, ('category_id', 'sort_order', 'topic_id'), '', '', ())`.

### `topics`

- primary key `pk_topics`: expected `(('id',),)`; missing.
- unexpected primary key `topics_pkey`: `(('id',),)`.
- unique constraint `uq_topics_domain_id_slug`: expected `(('domain_id', 'slug'),)`; missing.
- unexpected unique constraint `uq_topics_domain_slug`: `(('domain_id', 'slug'),)`.
- foreign key `fk_topics_domain_id_domains`: expected `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`; missing.
- unexpected foreign key `fk_topics_domain`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`.

### `user_notes`

- primary key `pk_user_notes`: expected `(('id',),)`; missing.
- unexpected primary key `user_notes_pkey`: `(('id',),)`.
- foreign key `fk_user_notes_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- foreign key `fk_user_notes_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_user_notes_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_user_notes_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_user_notes_user_id`: expected `(False, ('user_id',), '', '', ())`; missing.
- index `ix_user_notes_deleted_user_updated`: expected `(False, ('user_id', 'updated_at'), 'deleted_at is not null', '', ())`; missing.
- index `ix_user_notes_active_user_content`: expected `(False, ('user_id', 'content_item_id'), 'deleted_at is null', '', ())`; missing.
- index `ix_user_notes_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- unexpected index `ix_user_notes_recent`: `(False, ('user_id', 'updated_at', 'id'), '', '', ())`.

### `user_progress`

- primary key `pk_user_progress`: expected `(('user_id', 'content_item_id'),)`; missing.
- unexpected primary key `user_progress_pkey`: `(('user_id', 'content_item_id'),)`.
- check constraint `chk_user_progress_confidence`: expected `('confidence between 0 and 100',)`; different (actual `('confidence >= 0 and confidence <= 100',)`).
- foreign key `fk_user_progress_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- foreign key `fk_user_progress_content_item_id_content_items`: expected `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`; missing.
- unexpected foreign key `fk_user_progress_content_item`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`.
- unexpected foreign key `fk_user_progress_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_user_progress_content_item_id`: expected `(False, ('content_item_id',), '', '', ())`; missing.
- unexpected index `ix_user_progress_status`: `(False, ('user_id', 'status', 'updated_at', 'content_item_id'), '', '', ())`.

### `user_sync_change_log`

- primary key `pk_user_sync_change_log`: expected `(('user_id', 'cursor'),)`; missing.
- unexpected primary key `user_sync_change_log_pkey`: `(('user_id', 'cursor'),)`.
- foreign key `fk_user_sync_change_log_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_user_sync_change_log_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.
- index `ix_user_sync_change_log_retain_until`: expected `(False, ('retain_until',), '', '', ())`; missing.

### `user_sync_counters`

- primary key `pk_user_sync_counters`: expected `(('user_id',),)`; missing.
- unexpected primary key `user_sync_counters_pkey`: `(('user_id',),)`.
- foreign key `fk_user_sync_counters_user_id_profiles`: expected `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`; missing.
- unexpected foreign key `fk_user_sync_counters_user`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`.

## Enum and sequence comparison

- Enums: **14 expected; 14 exact; 0 different**.
- Enum names and labels: `block_type` = ['recognize', 'remember', 'invariant', 'approach', 'code', 'mistake', 'warning', 'diagram', 'table', 'architecture_flow', 'quiz', 'related_content', 'external_link']`; `change_operation` = ['upsert', 'delete']`; `content_relation_type` = ['prerequisite', 'related', 'alternative']`; `content_type` = ['problem', 'concept', 'pattern', 'article', 'architecture', 'case_study']`; `difficulty_level` = ['beginner', 'easy', 'medium', 'hard', 'expert']`; `learning_status` = ['new', 'learning', 'attempted', 'confident', 'mastered']`; `mutation_operation` = ['insert', 'update', 'delete']`; `mutation_status` = ['received', 'applied', 'rejected']`; `note_kind` = ['note', 'mistake', 'insight']`; `practice_outcome` = ['solved_independently', 'solved_with_hint', 'understood_not_coded', 'pattern_not_identified', 'skipped']`; `publication_status` = ['draft', 'in_review', 'published', 'archived']`; `release_status` = ['building', 'published', 'retired']`; `review_rating` = ['again', 'hard', 'good', 'easy']`; `topic_kind` = ['topic', 'pattern']`.
- Identity sequences: **6 expected and 6 present**: `activity_events_id_seq`, `content_version_status_history_id_seq`, `practice_providers_id_seq`, `profile_role_grants_id_seq`, `review_history_id_seq`, `roles_id_seq`.

## Functions, triggers, and extensions

- `refresh_content_version_search_document(uuid)`: present, but **not exact**; same-domain topic and category predicates are missing.
- Application-created triggers: expected 0; actual 0.
- `pg_trgm`: expected and present.
- Supabase-managed or extension-owned functions were observed but excluded from application mismatch counts.
- The migrations use `gen_random_uuid()`; Supabase supplies the supporting PostgreSQL capability. Alembic explicitly creates only `pg_trgm`.

## Expected final schema from migrations

Notation: `NN` = not nullable, `NULL` = nullable. Defaults and identity are shown when present.

### `activity_events`

**Columns**

- `id` `bigint` ? NN, identity
- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NULL
- `event_type` `varchar(80)` ? NN
- `source_entity_type` `varchar(80)` ? NULL
- `source_entity_id` `uuid` ? NULL
- `metadata` `jsonb` ? NULL
- `occurred_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_activity_events`: `(('id',),)`
- foreign key `fk_activity_events_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_activity_events_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'SET NULL')`
- index `ix_activity_events_content_item_id`: `(False, ('content_item_id',), '', '', ())`
- index `ix_activity_events_user_occurred`: `(False, ('user_id', 'occurred_at desc'), '', '', ())`

### `bookmarks`

**Columns**

- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `created_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_bookmarks`: `(('user_id', 'content_item_id'),)`
- foreign key `fk_bookmarks_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_bookmarks_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- index `ix_bookmarks_content_item_id`: `(False, ('content_item_id',), '', '', ())`

### `catalog_release_versions`

**Columns**

- `domain_id` `uuid` ? NN
- `catalog_release_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `content_version_id` `uuid` ? NN

**Constraints and indexes**

- primary key `pk_catalog_release_versions`: `(('catalog_release_id', 'content_item_id'),)`
- foreign key `fk_catalog_release_versions_item_version`: `(('content_item_id', 'content_version_id'), 'public', 'content_versions', ('content_item_id', 'id'), 'RESTRICT')`
- foreign key `fk_catalog_release_versions_domain_release`: `(('domain_id', 'catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'CASCADE')`
- foreign key `fk_catalog_release_versions_domain_item`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'RESTRICT')`
- index `ix_catalog_release_versions_domain_item`: `(False, ('domain_id', 'content_item_id'), '', '', ())`
- index `ix_catalog_release_versions_item_version`: `(False, ('content_item_id', 'content_version_id'), '', '', ())`
- index `ix_catalog_release_versions_domain_release`: `(False, ('domain_id', 'catalog_release_id'), '', '', ())`

### `catalog_releases`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `domain_id` `uuid` ? NN
- `release_number` `bigint` ? NN
- `status` `release_status` ? NN, default `building`
- `published_at` `timestamp with time zone` ? NULL
- `published_by` `uuid` ? NULL
- `retired_at` `timestamp with time zone` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_catalog_releases`: `(('id',),)`
- unique constraint `uq_catalog_releases_domain_number`: `(('domain_id', 'release_number'),)`
- unique constraint `uq_catalog_releases_domain_id_id`: `(('domain_id', 'id'),)`
- check constraint `chk_catalog_release_published_at`: `("status <> 'published' or published_at is not null",)`
- check constraint `chk_catalog_release_retire_after_publish`: `('retired_at is null or published_at is not null',)`
- check constraint `chk_catalog_release_number_positive`: `('release_number > 0',)`
- foreign key `fk_catalog_releases_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`
- foreign key `fk_catalog_releases_published_by_profiles`: `(('published_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- index `ix_catalog_releases_published_by`: `(False, ('published_by',), '', '', ())`

### `catalog_sync_change_log`

**Columns**

- `domain_id` `uuid` ? NN
- `cursor` `bigint` ? NN
- `entity_type` `varchar(80)` ? NN
- `entity_id` `uuid` ? NN
- `operation` `change_operation` ? NN
- `entity_version` `bigint` ? NULL
- `changed_at` `timestamp with time zone` ? NN, default `now()`
- `retain_until` `timestamp with time zone` ? NN

**Constraints and indexes**

- primary key `pk_catalog_sync_change_log`: `(('domain_id', 'cursor'),)`
- check constraint `chk_catalog_sync_change_retention`: `('retain_until > changed_at',)`
- check constraint `chk_catalog_sync_change_cursor_positive`: `('cursor > 0',)`
- foreign key `fk_catalog_sync_change_log_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`
- index `ix_catalog_sync_change_log_retain_until`: `(False, ('retain_until',), '', '', ())`

### `catalog_sync_counters`

**Columns**

- `domain_id` `uuid` ? NN
- `last_cursor` `bigint` ? NN, default `0`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_catalog_sync_counters`: `(('domain_id',),)`
- check constraint `chk_catalog_sync_counter_nonnegative`: `('last_cursor >= 0',)`
- foreign key `fk_catalog_sync_counters_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`

### `categories`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `domain_id` `uuid` ? NN
- `parent_category_id` `uuid` ? NULL
- `slug` `varchar(120)` ? NN
- `name` `varchar(160)` ? NN
- `description` `text` ? NULL
- `sort_order` `integer` ? NN, default `0`
- `is_active` `boolean` ? NN, default `true`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_categories`: `(('id',),)`
- unique constraint `uq_categories_domain_id_id`: `(('domain_id', 'id'),)`
- unique constraint `uq_categories_domain_id_slug`: `(('domain_id', 'slug'),)`
- check constraint `chk_category_not_self_parent`: `('parent_category_id is null or parent_category_id <> id',)`
- foreign key `fk_categories_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`
- foreign key `fk_categories_domain_parent_category`: `(('domain_id', 'parent_category_id'), 'public', 'categories', ('domain_id', 'id'), 'RESTRICT')`
- index `ix_categories_domain_parent`: `(False, ('domain_id', 'parent_category_id'), '', '', ())`

### `content_blocks`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `type` `block_type` ? NN
- `payload` `jsonb` ? NN
- `plain_text` `text` ? NULL
- `content_hash` `varchar(64)` ? NN
- `created_by` `uuid` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_content_blocks`: `(('id',),)`
- unique constraint `uq_content_blocks_type_hash`: `(('type', 'content_hash'),)`
- check constraint `chk_content_blocks_hash`: `("char_length(content_hash) = 64 and content_hash ~ '^[0-9a-f]{64}$'",)`
- foreign key `fk_content_blocks_created_by_profiles`: `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- index `ix_content_blocks_created_by`: `(False, ('created_by',), '', '', ())`

### `content_item_categories`

**Columns**

- `domain_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `category_id` `uuid` ? NN
- `sort_order` `integer` ? NN, default `0`

**Constraints and indexes**

- primary key `pk_content_item_categories`: `(('content_item_id', 'category_id'),)`
- foreign key `fk_content_item_categories_domain_category`: `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`
- foreign key `fk_content_item_categories_domain_item`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`
- index `ix_content_item_categories_domain_category`: `(False, ('domain_id', 'category_id'), '', '', ())`
- index `ix_content_item_categories_domain_item`: `(False, ('domain_id', 'content_item_id'), '', '', ())`

### `content_item_topics`

**Columns**

- `domain_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `topic_id` `uuid` ? NN
- `is_primary` `boolean` ? NN, default `false`
- `sort_order` `integer` ? NN, default `0`

**Constraints and indexes**

- primary key `pk_content_item_topics`: `(('content_item_id', 'topic_id'),)`
- foreign key `fk_content_item_topics_domain_topic`: `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`
- foreign key `fk_content_item_topics_domain_item`: `(('domain_id', 'content_item_id'), 'public', 'content_items', ('domain_id', 'id'), 'CASCADE')`
- index `uq_content_item_topics_one_primary`: `(True, ('content_item_id',), 'is_primary', '', ())`
- index `ix_content_item_topics_domain_topic`: `(False, ('domain_id', 'topic_id'), '', '', ())`
- index `ix_content_item_topics_domain_item`: `(False, ('domain_id', 'content_item_id'), '', '', ())`

### `content_items`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `domain_id` `uuid` ? NN
- `slug` `varchar(160)` ? NN
- `type` `content_type` ? NN
- `difficulty` `difficulty_level` ? NULL
- `current_published_version_id` `uuid` ? NULL
- `created_by` `uuid` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`
- `archived_at` `timestamp with time zone` ? NULL

**Constraints and indexes**

- primary key `pk_content_items`: `(('id',),)`
- unique constraint `uq_content_items_domain_id_id`: `(('domain_id', 'id'),)`
- unique constraint `uq_content_items_domain_id_slug`: `(('domain_id', 'slug'),)`
- foreign key `fk_content_items_current_published_version`: `(('id', 'current_published_version_id'), 'public', 'content_versions', ('content_item_id', 'id'), 'RESTRICT')`
- foreign key `fk_content_items_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`
- foreign key `fk_content_items_created_by_profiles`: `(('created_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- index `ix_content_items_current_version`: `(False, ('current_published_version_id',), '', '', ())`
- index `ix_content_items_current_ownership`: `(False, ('id', 'current_published_version_id'), '', '', ())`
- index `ix_content_items_public_browse`: `(False, ('domain_id', 'type'), 'archived_at is null', '', ())`
- index `ix_content_items_created_by`: `(False, ('created_by',), '', '', ())`

### `content_relations`

**Columns**

- `source_content_item_id` `uuid` ? NN
- `target_content_item_id` `uuid` ? NN
- `relation_type` `content_relation_type` ? NN
- `sort_order` `integer` ? NN, default `0`
- `created_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_content_relations`: `(('source_content_item_id', 'target_content_item_id', 'relation_type'),)`
- check constraint `chk_content_relation_not_self`: `('source_content_item_id <> target_content_item_id',)`
- check constraint `chk_content_relation_canonical_symmetric`: `("relation_type not in ('related', 'alternative')) or (source_content_item_id < target_content_item_id",)`
- foreign key `fk_content_relations_source`: `(('source_content_item_id',), 'public', 'content_items', ('id',), 'CASCADE')`
- foreign key `fk_content_relations_target`: `(('target_content_item_id',), 'public', 'content_items', ('id',), 'CASCADE')`
- index `ix_content_relations_target`: `(False, ('target_content_item_id',), '', '', ())`

### `content_version_blocks`

**Columns**

- `content_version_id` `uuid` ? NN
- `content_block_id` `uuid` ? NN
- `position` `integer` ? NN
- `heading` `varchar(240)` ? NULL

**Constraints and indexes**

- primary key `pk_content_version_blocks`: `(('content_version_id', 'position'),)`
- unique constraint `uq_content_version_blocks_version_block`: `(('content_version_id', 'content_block_id'),)`
- check constraint `chk_content_version_blocks_position`: `('position >= 0',)`
- foreign key `fk_content_version_blocks_version`: `(('content_version_id',), 'public', 'content_versions', ('id',), 'CASCADE')`
- foreign key `fk_content_version_blocks_block`: `(('content_block_id',), 'public', 'content_blocks', ('id',), 'RESTRICT')`
- index `ix_content_version_blocks_block`: `(False, ('content_block_id',), '', '', ())`

### `content_version_status_history`

**Columns**

- `id` `bigint` ? NN, identity
- `content_version_id` `uuid` ? NN
- `from_status` `publication_status` ? NULL
- `to_status` `publication_status` ? NN
- `changed_by` `uuid` ? NULL
- `reason` `text` ? NULL
- `changed_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_content_version_status_history`: `(('id',),)`
- foreign key `fk_content_version_status_history_changed_by`: `(('changed_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- foreign key `fk_content_version_status_history_version`: `(('content_version_id',), 'public', 'content_versions', ('id',), 'CASCADE')`
- index `ix_content_version_status_history_changed_by`: `(False, ('changed_by',), '', '', ())`
- index `ix_content_version_status_history_version`: `(False, ('content_version_id',), '', '', ())`

### `content_versions`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `content_item_id` `uuid` ? NN
- `version_number` `integer` ? NN
- `status` `publication_status` ? NN, default `draft`
- `title` `varchar(240)` ? NN
- `summary` `text` ? NULL
- `search_document` `tsvector` ? NULL
- `authored_by` `uuid` ? NULL
- `reviewed_by` `uuid` ? NULL
- `published_at` `timestamp with time zone` ? NULL
- `row_version` `bigint` ? NN, default `1`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`
- `published_by` `uuid` ? NULL

**Constraints and indexes**

- primary key `pk_content_versions`: `(('id',),)`
- unique constraint `uq_content_versions_content_item_id_id`: `(('content_item_id', 'id'),)`
- unique constraint `uq_content_versions_item_version`: `(('content_item_id', 'version_number'),)`
- check constraint `chk_content_version_published_at`: `("status <> 'published' or published_at is not null",)`
- check constraint `chk_content_version_row_version`: `('row_version > 0',)`
- check constraint `chk_content_version_number_positive`: `('version_number > 0',)`
- check constraint `chk_content_version_published_at_by`: `("status <> 'published' or (published_at is not null and published_by is not null)",)`
- foreign key `fk_content_versions_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'CASCADE')`
- foreign key `fk_content_versions_reviewed_by_profiles`: `(('reviewed_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- foreign key `fk_content_versions_authored_by_profiles`: `(('authored_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- foreign key `fk_content_versions_published_by_profiles`: `(('published_by',), 'public', 'profiles', ('id',), 'RESTRICT')`
- index `ix_content_versions_search_document`: `(False, ('search_document',), '', 'gin', ())`
- index `ix_content_versions_published_by`: `(False, ('published_by',), 'published_by is not null', '', ())`
- index `ix_content_versions_title_trgm`: `(False, ('title',), '', 'gin', (('title', 'gin_trgm_ops'),))`
- index `ix_content_versions_authored_by`: `(False, ('authored_by',), '', '', ())`
- index `ix_content_versions_reviewed_by`: `(False, ('reviewed_by',), '', '', ())`

### `device_catalog_sync_state`

**Columns**

- `device_id` `uuid` ? NN
- `domain_id` `uuid` ? NN
- `last_catalog_cursor` `bigint` ? NN, default `0`
- `last_catalog_release_id` `uuid` ? NULL
- `full_resync_required` `boolean` ? NN, default `false`
- `last_pull_at` `timestamp with time zone` ? NULL
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_device_catalog_sync_state`: `(('device_id', 'domain_id'),)`
- check constraint `chk_device_catalog_sync_cursor`: `('last_catalog_cursor >= 0',)`
- foreign key `fk_device_catalog_sync_state_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'CASCADE')`
- foreign key `fk_device_catalog_sync_state_device_id_devices`: `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`
- foreign key `fk_device_catalog_sync_state_domain_release`: `(('domain_id', 'last_catalog_release_id'), 'public', 'catalog_releases', ('domain_id', 'id'), 'RESTRICT')`
- index `ix_device_catalog_sync_state_domain_release`: `(False, ('domain_id', 'last_catalog_release_id'), '', '', ())`

### `device_user_sync_state`

**Columns**

- `device_id` `uuid` ? NN
- `last_user_cursor` `bigint` ? NN, default `0`
- `last_pull_at` `timestamp with time zone` ? NULL
- `last_push_at` `timestamp with time zone` ? NULL
- `full_resync_required` `boolean` ? NN, default `false`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_device_user_sync_state`: `(('device_id',),)`
- check constraint `chk_device_user_sync_cursor`: `('last_user_cursor >= 0',)`
- foreign key `fk_device_user_sync_state_device_id_devices`: `(('device_id',), 'public', 'devices', ('id',), 'CASCADE')`

### `devices`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `user_id` `uuid` ? NN
- `device_name` `varchar(160)` ? NULL
- `platform` `varchar(40)` ? NN
- `app_version` `varchar(40)` ? NULL
- `last_seen_at` `timestamp with time zone` ? NULL
- `registered_at` `timestamp with time zone` ? NN, default `now()`
- `revoked_at` `timestamp with time zone` ? NULL

**Constraints and indexes**

- primary key `pk_devices`: `(('id',),)`
- unique constraint `uq_devices_id_user`: `(('id', 'user_id'),)`
- foreign key `fk_devices_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- index `ix_devices_user_id`: `(False, ('user_id',), '', '', ())`

### `domains`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `slug` `varchar(100)` ? NN
- `name` `varchar(160)` ? NN
- `description` `text` ? NULL
- `sort_order` `integer` ? NN, default `0`
- `is_active` `boolean` ? NN, default `true`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_domains`: `(('id',),)`
- unique constraint `uq_domains_slug`: `(('slug',),)`

### `practice_attempts`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `practice_resource_id` `uuid` ? NULL
- `provider_id` `smallint` ? NN
- `outcome` `practice_outcome` ? NN
- `duration_seconds` `integer` ? NULL
- `hint_used` `boolean` ? NN, default `false`
- `confidence_before` `smallint` ? NULL
- `confidence_after` `smallint` ? NULL
- `attempted_at` `timestamp with time zone` ? NN
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `attempt_event_id` `uuid` ? NN

**Constraints and indexes**

- primary key `pk_practice_attempts`: `(('id',),)`
- unique constraint `uq_practice_attempts_attempt_event_id`: `(('attempt_event_id',),)`
- check constraint `chk_practice_attempt_conf_before`: `('confidence_before is null or confidence_before between 0 and 100',)`
- check constraint `chk_practice_attempt_conf_after`: `('confidence_after is null or confidence_after between 0 and 100',)`
- check constraint `chk_practice_attempt_duration`: `('duration_seconds is null or duration_seconds >= 0',)`
- foreign key `fk_practice_attempts_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_practice_attempts_provider_id_practice_providers`: `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`
- foreign key `fk_practice_attempts_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- foreign key `fk_practice_attempts_resource`: `(('practice_resource_id',), 'public', 'practice_resources', ('id',), 'RESTRICT')`
- foreign key `fk_practice_attempts_resource_item`: `(('practice_resource_id', 'content_item_id'), 'public', 'practice_resources', ('id', 'content_item_id'), 'RESTRICT')`
- foreign key `fk_practice_attempts_resource_provider`: `(('practice_resource_id', 'provider_id'), 'public', 'practice_resources', ('id', 'provider_id'), 'RESTRICT')`
- index `ix_practice_attempts_resource_item`: `(False, ('practice_resource_id', 'content_item_id'), '', '', ())`
- index `ix_practice_attempts_user_attempted`: `(False, ('user_id', 'attempted_at desc'), '', '', ())`
- index `ix_practice_attempts_resource_provider`: `(False, ('practice_resource_id', 'provider_id'), '', '', ())`
- index `ix_practice_attempts_content_item_id`: `(False, ('content_item_id',), '', '', ())`
- index `ix_practice_attempts_provider_id`: `(False, ('provider_id',), '', '', ())`
- index `ix_practice_attempts_resource_id`: `(False, ('practice_resource_id',), '', '', ())`

### `practice_providers`

**Columns**

- `id` `smallint` ? NN, identity
- `slug` `varchar(80)` ? NN
- `name` `varchar(120)` ? NN
- `base_url` `text` ? NULL
- `is_active` `boolean` ? NN, default `true`

**Constraints and indexes**

- primary key `pk_practice_providers`: `(('id',),)`
- unique constraint `uq_practice_providers_slug`: `(('slug',),)`

### `practice_resources`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `content_item_id` `uuid` ? NN
- `provider_id` `smallint` ? NN
- `external_key` `varchar(160)` ? NULL
- `url` `text` ? NN
- `url_hash` `varchar(64)` ? NN
- `title` `varchar(240)` ? NULL
- `is_primary` `boolean` ? NN, default `false`
- `sort_order` `integer` ? NN, default `0`
- `archived_at` `timestamp with time zone` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_practice_resources`: `(('id',),)`
- unique constraint `uq_practice_resources_id_provider`: `(('id', 'provider_id'),)`
- unique constraint `uq_practice_resources_item_provider_url`: `(('content_item_id', 'provider_id', 'url_hash'),)`
- unique constraint `uq_practice_resources_id_content_item`: `(('id', 'content_item_id'),)`
- check constraint `chk_practice_resources_url_hash`: `("char_length(url_hash) = 64 and url_hash ~ '^[0-9a-f]{64}$'",)`
- foreign key `fk_practice_resources_provider_id_practice_providers`: `(('provider_id',), 'public', 'practice_providers', ('id',), 'RESTRICT')`
- foreign key `fk_practice_resources_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- index `uq_practice_resources_one_primary`: `(True, ('content_item_id',), 'is_primary and archived_at is null', '', ())`
- index `ix_practice_resources_provider_id`: `(False, ('provider_id',), '', '', ())`

### `profile_role_grants`

**Columns**

- `id` `bigint` ? NN, identity
- `profile_id` `uuid` ? NN
- `role_id` `smallint` ? NN
- `granted_at` `timestamp with time zone` ? NN, default `now()`
- `granted_by` `uuid` ? NULL
- `revoked_at` `timestamp with time zone` ? NULL
- `revoked_by` `uuid` ? NULL

**Constraints and indexes**

- primary key `pk_profile_role_grants`: `(('id',),)`
- check constraint `chk_role_grant_revoke_time`: `('revoked_at is null or revoked_at >= granted_at',)`
- foreign key `fk_profile_role_grants_profile_id_profiles`: `(('profile_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_profile_role_grants_role_id_roles`: `(('role_id',), 'public', 'roles', ('id',), 'RESTRICT')`
- foreign key `fk_profile_role_grants_revoked_by_profiles`: `(('revoked_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- foreign key `fk_profile_role_grants_granted_by_profiles`: `(('granted_by',), 'public', 'profiles', ('id',), 'SET NULL')`
- index `ix_profile_role_grants_granted_by`: `(False, ('granted_by',), '', '', ())`
- index `ix_profile_role_grants_role_id`: `(False, ('role_id',), '', '', ())`
- index `ix_profile_role_grants_profile_id`: `(False, ('profile_id',), '', '', ())`
- index `ix_profile_role_grants_revoked_by`: `(False, ('revoked_by',), '', '', ())`

### `profiles`

**Columns**

- `id` `uuid` ? NN
- `display_name` `varchar(120)` ? NULL
- `avatar_url` `text` ? NULL
- `timezone` `varchar(64)` ? NN, default `utc`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_profiles`: `(('id',),)`
- foreign key `fk_profiles_id_users`: `(('id',), 'auth', 'users', ('id',), 'CASCADE')`

### `review_cards`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `scheduler_name` `varchar(80)` ? NN, default `simple`
- `scheduler_version` `varchar(40)` ? NN, default `1`
- `due_at` `timestamp with time zone` ? NN
- `interval_days` `numeric(12, 4)` ? NN, default `0`
- `stability` `numeric(12, 6)` ? NULL
- `difficulty` `numeric(12, 6)` ? NULL
- `review_count` `integer` ? NN, default `0`
- `lapse_count` `integer` ? NN, default `0`
- `last_reviewed_at` `timestamp with time zone` ? NULL
- `scheduler_state` `jsonb` ? NN, default `{}`
- `row_version` `bigint` ? NN, default `1`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`
- `suspended_at` `timestamp with time zone` ? NULL

**Constraints and indexes**

- primary key `pk_review_cards`: `(('id',),)`
- unique constraint `uq_review_cards_user_content`: `(('user_id', 'content_item_id'),)`
- unique constraint `uq_review_cards_id_user`: `(('id', 'user_id'),)`
- check constraint `chk_review_card_review_count`: `('review_count >= 0',)`
- check constraint `chk_review_card_lapse_count`: `('lapse_count >= 0',)`
- check constraint `chk_review_card_interval`: `('interval_days >= 0',)`
- check constraint `chk_review_card_row_version`: `('row_version > 0',)`
- foreign key `fk_review_cards_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- foreign key `fk_review_cards_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- index `ix_review_cards_content_item_id`: `(False, ('content_item_id',), '', '', ())`
- index `ix_review_cards_due_active`: `(False, ('user_id', 'due_at'), 'suspended_at is null', '', ())`

### `review_history`

**Columns**

- `id` `bigint` ? NN, identity
- `review_event_id` `uuid` ? NN, default `gen_random_uuid()`
- `review_card_id` `uuid` ? NN
- `user_id` `uuid` ? NN
- `rating` `review_rating` ? NN
- `reviewed_at` `timestamp with time zone` ? NN
- `response_time_ms` `integer` ? NULL
- `previous_due_at` `timestamp with time zone` ? NULL
- `next_due_at` `timestamp with time zone` ? NN
- `interval_days_after` `numeric(12, 4)` ? NULL
- `scheduler_name` `varchar(80)` ? NN
- `scheduler_version` `varchar(40)` ? NN
- `scheduler_state_after` `jsonb` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_review_history`: `(('id',),)`
- unique constraint `uq_review_history_review_event_id`: `(('review_event_id',),)`
- check constraint `chk_review_history_interval`: `('interval_days_after is null or interval_days_after >= 0',)`
- check constraint `chk_review_history_response_time`: `('response_time_ms is null or response_time_ms >= 0',)`
- foreign key `fk_review_history_card_user`: `(('review_card_id', 'user_id'), 'public', 'review_cards', ('id', 'user_id'), 'CASCADE')`
- index `ix_review_history_card_user`: `(False, ('review_card_id', 'user_id'), '', '', ())`
- index `ix_review_history_user_reviewed`: `(False, ('user_id', 'reviewed_at desc'), '', '', ())`

### `roles`

**Columns**

- `id` `smallint` ? NN, identity
- `code` `varchar(80)` ? NN
- `description` `text` ? NULL

**Constraints and indexes**

- primary key `pk_roles`: `(('id',),)`
- unique constraint `uq_roles_code`: `(('code',),)`

### `sync_mutations`

**Columns**

- `mutation_id` `uuid` ? NN
- `user_id` `uuid` ? NN
- `device_id` `uuid` ? NN
- `entity_type` `varchar(80)` ? NN
- `entity_id` `uuid` ? NN
- `operation` `mutation_operation` ? NN
- `payload` `jsonb` ? NULL
- `request_hash` `varchar(64)` ? NN
- `status` `mutation_status` ? NN, default `received`
- `base_row_version` `bigint` ? NULL
- `resulting_row_version` `bigint` ? NULL
- `error_code` `varchar(80)` ? NULL
- `received_at` `timestamp with time zone` ? NN, default `now()`
- `processed_at` `timestamp with time zone` ? NULL
- `expires_at` `timestamp with time zone` ? NN

**Constraints and indexes**

- primary key `pk_sync_mutations`: `(('mutation_id',),)`
- check constraint `chk_sync_mutation_request_hash`: `("char_length(request_hash) = 64 and request_hash ~ '^[0-9a-f]{64}$'",)`
- check constraint `chk_sync_mutation_status_consistency`: `("status = 'received' and processed_at is null) or (status = 'applied' and processed_at is not null) or (status = 'rejected' and processed_at is not null and error_code is not null",)`
- check constraint `chk_sync_mutation_expiry`: `('expires_at > received_at',)`
- check constraint `chk_sync_mutation_base_version`: `('base_row_version is null or base_row_version > 0',)`
- check constraint `chk_sync_mutation_result_version`: `('resulting_row_version is null or resulting_row_version > 0',)`
- check constraint `chk_sync_mutation_processed_time`: `('processed_at is null or processed_at >= received_at',)`
- foreign key `fk_sync_mutations_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_sync_mutations_device_user`: `(('device_id', 'user_id'), 'public', 'devices', ('id', 'user_id'), 'CASCADE')`
- index `ix_sync_mutations_expires_at`: `(False, ('expires_at',), '', '', ())`
- index `ix_sync_mutations_device_user`: `(False, ('device_id', 'user_id'), '', '', ())`
- index `ix_sync_mutations_user_id`: `(False, ('user_id',), '', '', ())`

### `topic_categories`

**Columns**

- `domain_id` `uuid` ? NN
- `topic_id` `uuid` ? NN
- `category_id` `uuid` ? NN
- `sort_order` `integer` ? NN, default `0`

**Constraints and indexes**

- primary key `pk_topic_categories`: `(('topic_id', 'category_id'),)`
- foreign key `fk_topic_categories_domain_category`: `(('domain_id', 'category_id'), 'public', 'categories', ('domain_id', 'id'), 'CASCADE')`
- foreign key `fk_topic_categories_domain_topic`: `(('domain_id', 'topic_id'), 'public', 'topics', ('domain_id', 'id'), 'CASCADE')`
- index `ix_topic_categories_domain_category`: `(False, ('domain_id', 'category_id'), '', '', ())`
- index `ix_topic_categories_domain_topic`: `(False, ('domain_id', 'topic_id'), '', '', ())`

### `topics`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `domain_id` `uuid` ? NN
- `kind` `topic_kind` ? NN, default `topic`
- `slug` `varchar(120)` ? NN
- `name` `varchar(160)` ? NN
- `description` `text` ? NULL
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_topics`: `(('id',),)`
- unique constraint `uq_topics_domain_id_id`: `(('domain_id', 'id'),)`
- unique constraint `uq_topics_domain_id_slug`: `(('domain_id', 'slug'),)`
- foreign key `fk_topics_domain_id_domains`: `(('domain_id',), 'public', 'domains', ('id',), 'RESTRICT')`

### `user_notes`

**Columns**

- `id` `uuid` ? NN, default `gen_random_uuid()`
- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `kind` `note_kind` ? NN
- `title` `varchar(240)` ? NULL
- `body` `text` ? NN
- `row_version` `bigint` ? NN, default `1`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`
- `deleted_at` `timestamp with time zone` ? NULL

**Constraints and indexes**

- primary key `pk_user_notes`: `(('id',),)`
- check constraint `chk_user_note_row_version`: `('row_version > 0',)`
- foreign key `fk_user_notes_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- foreign key `fk_user_notes_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- index `ix_user_notes_user_id`: `(False, ('user_id',), '', '', ())`
- index `ix_user_notes_deleted_user_updated`: `(False, ('user_id', 'updated_at'), 'deleted_at is not null', '', ())`
- index `ix_user_notes_active_user_content`: `(False, ('user_id', 'content_item_id'), 'deleted_at is null', '', ())`
- index `ix_user_notes_content_item_id`: `(False, ('content_item_id',), '', '', ())`

### `user_progress`

**Columns**

- `user_id` `uuid` ? NN
- `content_item_id` `uuid` ? NN
- `status` `learning_status` ? NN, default `new`
- `confidence` `smallint` ? NN, default `0`
- `last_opened_at` `timestamp with time zone` ? NULL
- `row_version` `bigint` ? NN, default `1`
- `created_at` `timestamp with time zone` ? NN, default `now()`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_user_progress`: `(('user_id', 'content_item_id'),)`
- check constraint `chk_user_progress_row_version`: `('row_version > 0',)`
- check constraint `chk_user_progress_confidence`: `('confidence between 0 and 100',)`
- foreign key `fk_user_progress_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- foreign key `fk_user_progress_content_item_id_content_items`: `(('content_item_id',), 'public', 'content_items', ('id',), 'RESTRICT')`
- index `ix_user_progress_content_item_id`: `(False, ('content_item_id',), '', '', ())`

### `user_sync_change_log`

**Columns**

- `user_id` `uuid` ? NN
- `cursor` `bigint` ? NN
- `entity_type` `varchar(80)` ? NN
- `entity_id` `uuid` ? NN
- `operation` `change_operation` ? NN
- `entity_version` `bigint` ? NULL
- `changed_at` `timestamp with time zone` ? NN, default `now()`
- `retain_until` `timestamp with time zone` ? NN

**Constraints and indexes**

- primary key `pk_user_sync_change_log`: `(('user_id', 'cursor'),)`
- check constraint `chk_user_sync_change_cursor_positive`: `('cursor > 0',)`
- check constraint `chk_user_sync_change_retention`: `('retain_until > changed_at',)`
- foreign key `fk_user_sync_change_log_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`
- index `ix_user_sync_change_log_retain_until`: `(False, ('retain_until',), '', '', ())`

### `user_sync_counters`

**Columns**

- `user_id` `uuid` ? NN
- `last_cursor` `bigint` ? NN, default `0`
- `updated_at` `timestamp with time zone` ? NN, default `now()`

**Constraints and indexes**

- primary key `pk_user_sync_counters`: `(('user_id',),)`
- check constraint `chk_user_sync_counter_nonnegative`: `('last_cursor >= 0',)`
- foreign key `fk_user_sync_counters_user_id_profiles`: `(('user_id',), 'public', 'profiles', ('id',), 'CASCADE')`

## Proposed later clean-rebuild procedure (not executed)

### Backup requirements

1. Stop all writers.
2. Take a full logical backup and a schema-only backup using the Session Pooler credentials through environment variables; never place credentials in command history.
3. Verify both backup files are non-empty and record checksums.
4. Re-run row counts immediately before approval.

### Exact commands to run only after approval

```powershell
cd D:\Code\RecallStack\Code\backend
# Create verified backups first using pg_dump with credentials supplied via environment variables.
# Run a reviewed SQL cleanup file containing only the application objects listed below.
psql -v ON_ERROR_STOP=1 -f scripts/drop_recallstack_public_objects.sql
uv run alembic upgrade head
uv run alembic current
uv run python -m recallstack.commands.seed
$env:RUN_INTEGRATION_TESTS="1"
uv run pytest
```

The reviewed cleanup SQL must explicitly drop only these application tables (with dependency-aware `CASCADE`), the application enum types, and `refresh_content_version_search_document(uuid)`. It must not use `DROP SCHEMA public`, must not drop `pg_trgm`, and must not address any managed schema.

Application tables affected: `activity_events`, `bookmarks`, `catalog_release_versions`, `catalog_releases`, `catalog_sync_change_log`, `catalog_sync_counters`, `categories`, `content_blocks`, `content_item_categories`, `content_item_topics`, `content_items`, `content_relations`, `content_version_blocks`, `content_version_status_history`, `content_versions`, `device_catalog_sync_state`, `device_user_sync_state`, `devices`, `domains`, `practice_attempts`, `practice_providers`, `practice_resources`, `profile_role_grants`, `profiles`, `review_cards`, `review_history`, `roles`, `sync_mutations`, `topic_categories`, `topics`, `user_notes`, `user_progress`, `user_sync_change_log`, `user_sync_counters`.

Application enum types affected: `block_type`, `change_operation`, `content_relation_type`, `content_type`, `difficulty_level`, `learning_status`, `mutation_operation`, `mutation_status`, `note_kind`, `practice_outcome`, `publication_status`, `release_status`, `review_rating`, `topic_kind`.

### Rollback plan

1. Keep the application stopped.
2. Drop only the newly recreated RecallStack application objects using the reviewed cleanup script.
3. Restore the verified pre-change logical backup.
4. Validate schema object counts, row counts, foreign keys, and application health.
5. Do not restore or modify Supabase-managed schemas.

## Safety confirmation

- Inspection was limited to `public` catalogs and `count(*)` on `public` tables.
- No user rows or authentication records were selected.
- No DDL, DML, migration, stamp, truncate, rename, or managed-schema operation was performed.
- The proposed approach remains unexecuted pending explicit approval.
